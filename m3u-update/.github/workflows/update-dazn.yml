name: Update DAZN ELEVEN PT M3U

on:
  schedule:
    - cron: "*/5 * * * *"   # كل 5 دقائق
  workflow_dispatch:        # تشغيل يدوي إذا تحب

permissions:
  contents: write

jobs:
  update-dazn:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install requests

      - name: Run update_dazn_pt.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SOURCE_URL: "https://raw.githubusercontent.com/DisabledAbel/daddylivehd-m3u/f582ae100c91adf8c8db905a8f97beb42f369a0b/daddylive-events.m3u8"
          DEST_RAW_URL: "https://raw.githubusercontent.com/a7shk1/m3u-broadcast/refs/heads/main/dazn.m3u"
          GITHUB_REPO: "a7shk1/m3u-broadcast"
          GITHUB_BRANCH: "main"
          DEST_REPO_PATH: "dazn.m3u"
          COMMIT_MESSAGE: "🔄 auto-update dazn.m3u (DAZN ELEVEN PT 1/2/3)"
        run: python scripts/update_dazn_pt.py
